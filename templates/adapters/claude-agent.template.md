---
name: "{{NAME}}"
description: "{{DESCRIPTION}}"
tools: "{{TOOLS}}"
model: "{{MODEL}}"
forge-generated: true
canonical-path: "{{CANONICAL_PATH}}"
canonical-revision: "{{CANONICAL_REVISION}}"
---

Read the canonical agent role at `{{CANONICAL_PATH}}` completely, then perform the assigned task
under that contract. This wrapper does not redefine the role.
